---
interface Props {
    path: string;
    language: any;
    style?: string;
}

const { path, language, style } = Astro.props;

import { Code } from 'astro:components';
import fs from 'node:fs';

const File = fs.readFileSync(path, 'utf8');
---

<Code code={File} lang={language} style={style}/>