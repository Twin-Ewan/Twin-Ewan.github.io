---
const { file, fallback } = Astro.props
---

<object data={file} type="application/pdf" class="PDF" width="100%" height="100%">
    {fallback ? <slot/> : <a> href={file}>Download PDF</a>}
</object>